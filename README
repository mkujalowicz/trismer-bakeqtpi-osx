For OSX:

brew install ctorrent
brew install mpfr gmp libmpc libelf texinfo --env=std #env=std needed due to mpfr failing to compile

For Ubuntu:

sudo apt-get install build-essential git ia32-libs ctorrent unzip
